{
    "name": "tasks-manager-dev",
    "private": true,
    "scripts": {
    "dev:api": "dotnet watch --project api run",
    "dev:web": "npm --prefix web run dev -- --open",
    "dev": "concurrently -k -n API,WEB \"npm:dev:api\" \"npm:dev:web\""
  },
    "devDependencies": {
      "concurrently": "^9.0.0"
    }
  }
  